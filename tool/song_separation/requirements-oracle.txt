# Development oracle only. Torch and torchaudio are intentionally supplied by
# the caller because their wheels depend on CPU/CUDA and platform selection.
openunmix==1.3.0
onnx==1.22.0
onnxruntime==1.29.0
