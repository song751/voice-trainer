include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Cargokit is an FRB-generated nested Dart package with its own pubspec.
    # It is built by its own toolchain and must not be analyzed as app source.
    - rust_builder/cargokit/build_tool/**
